apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "pixel-war.fullname" . }}-db-pvc
  labels:
    {{- include "pixel-war.labels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.db.storage }}
